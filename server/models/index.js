// import in all the models
const User = require('./User');
const Employer = require('./Employer');
const Job = require('./Job');

// export models out to be used
module.exports = { User, Employer, Job };